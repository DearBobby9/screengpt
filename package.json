{
  "name": "screengpt",
  "private": true,
  "version": "0.1.0",
  "description": "A lightweight macOS desktop AI assistant with screenshot and vision capabilities",
  "scripts": {
    "setup": "cd nextchat && npm install",
    "dev": "cd src-tauri && cargo tauri dev",
    "build": "cd src-tauri && cargo tauri build",
    "tauri": "cd src-tauri && cargo tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
